{
  "buildCommand": "cd Frontend-Body && npm install && npm run build && cd.. && cd Backend-Soul && npm run build",
  "outputDirectory": "Frontend-Body/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/Backend-Soul/dist/index.js"
    }
  ]
}
